# 更新日志（Changelog）

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.1] - 2026-09-11

### 修复
- **代理/安全软件环境下余额查询报「证书校验失败」**：本机 HTTPS 信任库只用了 certifi，
  缺少代理重新签发流量所用的根证书（该根证书装在 Windows 证书库里，所以浏览器访问正常，
  只有本程序失败）。现在启动时把 Windows 系统证书库与 certifi 合并后全局生效，
  **证书校验保持开启**（不降级为跳过校验），余额查询 / 翻译 / 检查更新一并恢复；
- **DeepSeek 模型名已停服**：`deepseek-chat` / `deepseek-reasoner` 已于 2026-07-24 停止服务，
  现行模型名为 `deepseek-flash`（2026-09-10 发布 V4.1-Flash）；旧配置自动迁移，
  自建/第三方 OpenAI 兼容接口上的同名模型不受影响；
- 余额查询失败时给出可照做的提示（HTTP 状态码 + 服务端返回的原因），
  404 时回退 `/v1/user/balance`；账户同时有人民币与美元时优先显示人民币。

### 说明
- 余额接口地址 `GET https://api.deepseek.com/user/balance` 与 BASE URL 均未变更。

## [1.0.0] - 2026-08-31

### 新增
- 右键轮盘菜单（翻译 / 录屏 / 设置），支持外甩取消、动画速度三档、暗/亮双主题；
- 选中文本直接翻译；无选中文本时框选截图 → OCR → 翻译；
- 翻译结果浮窗：独立主题、可拖拽、可调整大小并记忆；
- 区域录屏（ffmpeg + gdigrab）：MP4/GIF、15/30/60 FPS、停止按钮带计时器；
- DeepSeek 余额 / 今日消费查询（按当日余额差值统计，忽略充值增长）；
- 系统托盘：双击打开设置、开机自启动、打开录屏文件位置；
- 设置面板五页（翻译 / 轮盘与识别 / 录屏 / DeepSeek / 通用）；
- RapidOCR 可选安装（设置内一键安装/卸载）。

### 修复
- 录屏区域宽高为奇数时输出 0KB（自动取偶）；
- pythonw 任务栏显示 Python 图标（AppUserModelID + 原生图标注入）；
- 设置面板勾选框、下拉箭头样式与整体风格统一。
- 单个英文单词（如 upper）被 MyMemory 返回原文时误报成功，
  现自动跳过并切换其他翻译源（Google / Edge / DeepSeek）。

### 说明
- 大量参考 [StarPie](https://github.com/SoftBlack42/StarPie) 的美术风格与交互设计。
